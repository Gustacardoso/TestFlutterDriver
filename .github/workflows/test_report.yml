name: Test Report

on:
  push:
    branches: [ master]

jobs:
  flutter_test:
    name: Run flutter test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: '12.x'
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: '2.2.2'
      - run: dart pub get
      - run: flutter test test_driver/bdd

  build_appapk:
    name: Build Flutter (Android)
    needs: [flutter_test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"
      - run: flutter clean
      - run: flutter test test_driver/bdd
      - name: Upload apk
        uses: actions/upload-artifact@v2.2.4
        with:
          name: apk
          path: build/app/outputs/flutter-apk/report.json
          retention-days: 5
  